import { NextResponse } from "next/server"; import { getSupabaseEnvironment } from "@/lib/supabase/env";
export async function POST(request:Request){try{const {email,password}=await request.json() as {email:string;password:string};const {url,anonKey}=getSupabaseEnvironment();const result=await fetch(`${url}/auth/v1/signup`,{method:"POST",headers:{apikey:anonKey,"Content-Type":"application/json"},body:JSON.stringify({email,password})});if(!result.ok)return NextResponse.json({message:"Unable to create account."},{status:400});return NextResponse.json({redirect:"/verify-email"});}catch{return NextResponse.json({message:"Registration is unavailable."},{status:500});}}
