import { NextResponse } from "next/server";
import { getSessionUserId } from "@/lib/auth/session";
import { supabaseRequest } from "@/lib/supabase/server";
import { resolveActiveOrganization } from "@/services/organization-service";
export async function GET(){const userId=await getSessionUserId();if(!userId)return NextResponse.json({message:"Sign in to load your workspace."},{status:401});try{const workspace=await resolveActiveOrganization(userId);const id=workspace.organization.id;const [projects,tasks,leads,meetings,tickets]=await Promise.all([supabaseRequest<{id:string}[]>(`projects?organization_id=eq.${id}&archived_at=is.null&status=neq.completed&select=id`),supabaseRequest<{id:string}[]>(`tasks?organization_id=eq.${id}&status=not.in.(completed,done)&select=id`),supabaseRequest<{estimated_value:number|null}[]>(`leads?organization_id=eq.${id}&stage=not.in.(won,lost)&select=estimated_value`),supabaseRequest<{id:string}[]>(`meetings?organization_id=eq.${id}&starts_at=gte.${new Date().toISOString()}&select=id`),supabaseRequest<{id:string}[]>(`support_requests?organization_id=eq.${id}&status=not.in.(resolved,closed)&select=id`)]);return NextResponse.json({organization:workspace.organization,role:workspace.role,metrics:{projects:projects.length,tasks:tasks.length,pipeline:leads.reduce((sum,x)=>sum+Number(x.estimated_value??0),0),meetings:meetings.length,tickets:tickets.length}});}catch(cause){if(process.env.NODE_ENV==="development")console.error("Workspace summary failed",cause);return NextResponse.json({message:"Unable to load workspace. Please try again."},{status:500});}}
