"use client";
import { useState } from "react";
export default function AssistantPage(){const [message,setMessage]=useState(""),[answer,setAnswer]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);async function ask(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");try{const r=await fetch("/api/ai/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message})});const b=await r.json();if(!r.ok)throw Error(b.message);setAnswer(b.answer)}catch(e){setError(e instanceof Error?e.message:"Unable to reach assistant.")}finally{setLoading(false)}}return <main className="app"><section className="content"><div className="page"><section className="intro"><div><p className="eyebrow">WORKSPACE ASSISTANT</p><h1>Opervia AI</h1><p>Answers are limited to records you can access in this workspace.</p></div></section><section className="card project-card"><div className="assistant-chat">{answer?<div className="ai-message">{answer}</div>:<div className="empty-state"><b>Ask about your operations.</b><p>Try “Show overdue tasks” or “Which leads need follow-up today?”</p></div>}</div><form className="inline-form" onSubmit={ask}><input value={message} onChange={e=>setMessage(e.target.value)} placeholder="Ask about your workspace" required/><button className="primary-button" disabled={loading}>{loading?"Thinking…":"Ask AI"}</button></form>{error&&<p className="form-error">{error}</p>}</section></div></section></main>}
