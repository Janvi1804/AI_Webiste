import { NextRequest, NextResponse } from "next/server";
import { isDemoMode } from "@/lib/demo";
export function proxy(request:NextRequest){ const {pathname}=request.nextUrl; if(isDemoMode){ if(pathname==="/"||pathname==="/login"||pathname==="/register"||pathname==="/forgot-password") return NextResponse.redirect(new URL("/app",request.url)); return NextResponse.next(); } if(pathname==="/") return NextResponse.redirect(new URL("/app",request.url)); const userId=request.cookies.get("sb-access-token")?.value; if(pathname.startsWith("/app")&&!userId)return NextResponse.redirect(new URL(`/login?next=${encodeURIComponent(pathname)}`,request.url)); if((pathname==="/login"||pathname==="/register")&&userId)return NextResponse.redirect(new URL("/app",request.url)); return NextResponse.next(); }
export const config={matcher:["/","/app/:path*","/login","/register","/forgot-password"]};
